# PROJECT KNOWLEDGE BASE

**Generated:** 2026-04-23
**Commit:** 1fa52bd
**Branch:** main

## OVERVIEW
SIO-MMOS丛书 static site — 纯静态HTML/CSS/JS书籍展示与阅读器，无构建系统。  
另含 **Axiom-Alpha**：投资第一性原理 Agent Team（Cursor Skills + Subagents）。

## STRUCTURE
```
.
├── SSIO-BOOK/          # 丛书Markdown源文件（33个.md）
├── book-show/          # 网站前端（所有代码）
├── axiom-alpha/        # 投资决策认知有机体（知识库/契约/脚本）
├── .cursor/
│   ├── agents/         # Axiom-Alpha Agent Team（10 subagents）
│   ├── skills/         # PARR 循环与专项 skills
│   └── rules/          # axiom-alpha 作用域规则
├── CLAUDE.md
└── *.md
```

## AXIOM-ALPHA（投资 Agent Team）
| Task | Location | Notes |
|------|----------|-------|
| 完整决策循环 | `/investment-decision-loop` | PARR：意图→规划→召唤→免疫→决策锚点 |
| 知识库 | `axiom-alpha/knowledge/` | 五大公理、十大定理、融合分析模型 |
| Subagents | `.cursor/agents/` | intent/planner/experts/immune/dialectic/decision |
| 校验 | `axiom-alpha/scripts/` | schema 校验 + 先天免疫检查 |
| 详述 | `axiom-alpha/AGENTS.md` | 团队角色与硬约束 |

## WHERE TO LOOK
| Task | Location | Notes |
|------|----------|-------|
| 修改营销页 | `book-show/index.html` | 样式全内联，无外部CSS |
| 修改书籍展示页 | `book-show/book-show.html` + `book-show.js` | 依赖 chapters-data.js |
| 修改阅读器 | `book-show/reader.html` + `reader.js` | 依赖 marked.min.js |
| 添加/修改章节 | `SSIO-BOOK/` 对应目录 | 同步更新 `reader.js` 的 `chapterMapping` |
| 修改章节预览数据 | `book-show/chapters-data.js` | 模态框内容源 |
| 修改样式变量 | `book-show/*.css` 的 `:root` | reader.css 支持 data-theme="dark/sepia" |
| 修改字体/主题色 | 所有CSS文件的 `:root` | 4套渐变主题色重复定义 |

## CODE MAP

| Symbol | Type | Location | Role |
|--------|------|----------|------|
| `chapterMapping` | Object | `reader.js:4` | 卷→章节文件路径映射（硬编码） |
| `loadChapter()` | Function | `reader.js:144` | Fetch Markdown → marked.parse() |
| `switchVolume()` | Function | `reader.js:109` | 卷切换+目录重渲染 |
| `chaptersData` | Object | `chapters-data.js:2` | 章节元数据（模态框用） |
| `Particle` | Class | `book-show.js:12` | Hero canvas 粒子动画 |
| `drawKnowledgeGraph()` | Function | `book-show.js:129` | 知识图谱 canvas 动画 |
| `drawDiagram()` | Function | `book-show.js:324` | 模态框概念图示（canvas） |
| `generate.js` | Script | `book-show/generate.js` | Node.js 内联样式生成工具 |

## CONVENTIONS
- **无构建系统**：直接编辑HTML/CSS/JS，浏览器即运行
- **index.html 特殊**：唯一全内联样式+脚本的页面（book-show.html 和 reader.html 用外部文件）
- **中文文件名**：Markdown 源文件含中文与空格，reader.js 中 `chapterMapping` 硬编码这些路径
- **CSS变量命名**：`--bg-primary`, `--text-primary`, `--gradient-1` 等语义化命名
- **Canvas动画**：所有可视化（粒子、知识图谱、模态框图示）均用原生Canvas 2D，无第三方库
- **marked.min.js vendored**：本地拷贝，非npm引入

## ANTI-PATTERNS (THIS PROJECT)
- **勿编辑 `marked.min.js`** — 标有 "DO NOT EDIT THIS FILE"
- **勿在 reader.js 外修改章节路径** — `chapterMapping` 是唯一数据源
- **勿删除 generate.js** — 用于重新生成 index.html 内联样式（尽管目前可手动维护）

## UNIQUE STYLES
- `data-theme` 属性切换：reader 支持 `light` / `dark` / `sepia` 三主题
- 本地存储持久化：阅读进度、字体大小、主题、书签均存 `localStorage`
- 纯静态部署：任意HTTP服务器即可，`fetch()` 加载同级目录 `../SSIO-BOOK/` 的Markdown

## COMMANDS
```bash
# 启动本地服务器
python -m http.server 8000
npx http-server -p 8000
php -S localhost:8000

# 访问
open http://localhost:8000/book-show/
```

## NOTES
- Markdown文件使用中文命名且含空格，移动/重命名需同步更新 `reader.js` 的 `chapterMapping`
- `book-show.js` 同时服务于 `book-show.html`（粒子动画、知识图谱、Tab切换、模态框）
- `reader.js` 的 `chapterMapping` 路径使用 `../SSIO-BOOK/` 相对路径，部署时目录结构不可变
- 无 Service Worker / PWA 功能（注释中标记为"待实现"）
